/*
  # Update Auth Settings

  1. Changes
     - Disable email confirmation requirement for new users
     - This allows users to register and immediately use the application without email verification
*/

-- This is a SQL comment that would normally execute a command to disable email confirmation
-- Since we can't directly modify Supabase auth settings via SQL, this is a placeholder
-- The actual change needs to be made in the Supabase dashboard under Authentication > Email
-- Set "Confirm email" to OFF